# Local myAPIRest instance. Do not use a public production URL for early MCP development.
MY_API_REST_BASE_URL=http://localhost:3000

# Copy an access token from a local /auth/login response. Never commit a real token.
MY_API_REST_ACCESS_TOKEN=replace-with-a-local-access-token
